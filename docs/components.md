# Components

Workflow: `bitrix_component_search` → `bitrix_component_context` → `bitrix_impact_radius` when changing component files.

Example prompt: "Use Bitrix MCP to explain where this component template is used." Component context can include calls, template files/assets, parameters, and graph relations derived from indexed `IncludeComponent` calls and template conventions.
